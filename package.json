{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.0"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "landingpage/**/*.{js,jsx,ts,tsx,json,css,md}": [
      "cd landingpage && npm run format",
      "cd landingpage && npm run lint --fix"
    ]
  }
}
